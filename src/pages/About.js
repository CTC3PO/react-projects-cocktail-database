import React from "react";

export default function About() {
  return (
    <section className="section about-section">
      <h1 className="section-title">about us</h1>
      <p>
        The cocktail database is a simple web app that get cocktails from the
        TheCocktailDB.com APIs. The app displays the recipe details and also let
        user search for their favorite recipe. The web app is hosted on netlify.
      </p>
    </section>
  );
}
